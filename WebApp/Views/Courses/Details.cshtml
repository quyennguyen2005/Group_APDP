@model WebApp.ViewModels.CourseDetailsViewModel

@{
    ViewData["Title"] = "Chi tiết khoá học";
    var flash = TempData["CourseMessage"] as string;
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <p class="text-uppercase text-muted mb-1">Khoá học</p>
        <h1 class="display-6 mb-0">@Model.Course.Title</h1>
        <div class="text-muted">@Model.Course.CourseCode • @Model.Course.Semester</div>
    </div>
    <div class="d-flex gap-2">
        <a asp-action="Edit" asp-route-id="@Model.Course.Id" class="btn btn-outline-secondary">Sửa</a>
        <a asp-action="Index" class="btn btn-outline-secondary">Quay lại</a>
    </div>
</div>

@if (!string.IsNullOrEmpty(flash))
{
    <div class="alert alert-info">@flash</div>
}

<div class="row g-4 mb-4">
    <div class="col-md-4">
        <div class="feature-card text-start">
            <div class="feature-icon">👨‍🏫</div>
            <div class="feature-title">Giảng viên</div>
            <div>@Model.Course.Instructor</div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="feature-card text-start">
            <div class="feature-icon">🗓️</div>
            <div class="feature-title">Thời gian</div>
            <div>@Model.Course.StartDate:dd/MM/yyyy - @Model.Course.EndDate:dd/MM/yyyy</div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="feature-card text-start">
            <div class="feature-icon">👥</div>
            <div class="feature-title">Sĩ số</div>
            <div>@Model.EnrolledStudents.Count / @Model.Course.MaxStudents sinh viên</div>
        </div>
    </div>
</div>

<div class="row g-4">
    <div class="col-lg-7">
        <div class="card shadow-sm">
            <div class="card-header d-flex justify-content-between align-items-center">
                <span class="fw-semibold">Sinh viên đang theo học</span>
            </div>
            <div class="card-body">
                @if (!Model.EnrolledStudents.Any())
                {
                    <div class="text-muted">Chưa có sinh viên nào được phân công.</div>
                }
                else
                {
                    <div class="list-group list-group-flush">
                        @foreach (var student in Model.EnrolledStudents)
                        {
                            <div class="list-group-item d-flex justify-content-between align-items-center">
                                <div>
                                    <div class="fw-semibold">@student.FullName</div>
                                    <div class="text-muted small">@student.StudentCode • @student.Major</div>
                                </div>
                                <form asp-action="RemoveStudent" method="post" class="ms-3">
                                    <input type="hidden" name="courseId" value="@Model.Course.Id" />
                                    <input type="hidden" name="studentId" value="@student.Id" />
                                    <button type="submit" class="btn btn-sm btn-outline-danger">Xoá</button>
                                </form>
                            </div>
                        }
                    </div>
                }
            </div>
        </div>
    </div>
    <div class="col-lg-5">
        <div class="card shadow-sm">
            <div class="card-header">
                <span class="fw-semibold">Phân công sinh viên</span>
            </div>
            <div class="card-body">
                @if (!Model.AvailableStudents.Any())
                {
                    <div class="text-muted">Mọi sinh viên đã được phân công.</div>
                }
                else
                {
                    <form asp-action="AssignStudent" method="post" class="d-flex flex-column gap-3">
                        <input type="hidden" name="courseId" value="@Model.Course.Id" />
                        <select class="form-select" name="selectedStudentId">
                            <option value="">-- Chọn sinh viên --</option>
                            @foreach (var student in Model.AvailableStudents)
                            {
                                <option value="@student.Id">@student.FullName (@student.StudentCode)</option>
                            }
                        </select>
                        <button type="submit" class="btn btn-primary">Thêm vào khoá học</button>
                    </form>
                }
            </div>
        </div>
    </div>
</div>
