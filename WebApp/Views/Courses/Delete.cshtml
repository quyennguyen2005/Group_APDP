@model WebApp.Models.Course

@{
    ViewData["Title"] = "Xoá khoá học";
}

<div class="alert alert-warning">
    Bạn chắc chắn muốn xoá khoá học <strong>@Model.Title (@Model.CourseCode)</strong>? Tất cả phân công sinh viên sẽ bị xoá theo.
</div>

<form asp-action="Delete" method="post" class="d-flex gap-2">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Xoá</button>
    <a asp-action="Index" class="btn btn-outline-secondary">Huỷ</a>
</form>

